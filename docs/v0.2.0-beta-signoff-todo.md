# v0.2.0-beta.0 Signoff TODO

Generated: 2026-06-26 CST

This file tracks the remaining acceptance work before promoting sepigs beyond controlled beta testing. It does not claim production-stable status.

## 1. Complete 24-Hour Soak

Current status: `pending`.

Beta Signoff Sprint status: `running-in-screen`.

Current live run:

- Session: `screen -r sepigs-24h`
- Log: `reports/soak/24h/screen.log`
- Metrics: `reports/soak/24h/metrics.jsonl`
- Early checkpoint: about 420,881 ms, 71,467 successes, 0 runner errors, 84 reloads.

Existing evidence:

- Two-hour checkpointed `24h` profile segment passed.
- Successful requests: `1,220,350/1,220,350`.
- Errors: `0`.
- Reloads: `1,440`.
- Final sockets/timers/listeners: `0/0/0`.
- Report: `docs/soak-24h-v0.2.0-beta-report.md`.
- Run directory: `reports/soak/24h/`.

Resume command:

```bash
npm run soak:resume -- --profile 24h --duration-ms 86400000 --run-dir reports/soak/24h --docs-report docs/soak-24h-v0.2.0-beta-report.md
```

Fresh full-run command:

```bash
SEPIGS_TEST_HOST=127.0.0.1 SEPIGS_TEST_PORT=0 SEPIGS_DISABLE_IPV6=1 npm run soak:24h
```

Pass criteria:

- Fatal errors: `0`.
- Error rate: `< 0.1%`.
- Stop cleanup: sockets/timers/listeners = `0/0/0`.
- RSS does not grow without bound.
- Heap does not grow without bound.
- p95 latency does not continuously degrade.
- Mixed scenarios include fake-IP, UDP, DNS, Dashboard read-only metrics, route reload, direct, and block paths.

Fail criteria:

- Any fatal process crash.
- Error rate `>= 0.1%`.
- Resource counters remain non-zero after stop.
- Clear unbounded RSS/heap growth.
- Sustained p95 degradation without recovery.

## 2. Human Client Signoff

Use `artifacts/sepigs-v0.2.0-client-verification-pack.zip`.

Each signoff row must record client name, client version, device, OS version, config file, test URL, HTTP result, HTTPS result, SOCKS5 result, auth result, DNS/fake-IP involvement, failure log or screenshot description, and one conclusion: `verified`, `partially verified`, `failed`, `blocked`, or `not tested`.

| Priority | Client | Config / worksheet | Current status | Required result |
| --- | --- | --- | --- | --- |
| P0 | Chrome / system proxy | `verification/chrome-system.md` | not tested | verified or documented failure |
| P0 | Mihomo | `verification/mihomo.md` | not tested | verified or documented failure |
| P0 | Shadowrocket | `verification/shadowrocket.md` | not tested | verified or documented failure |
| P1 | Clash Verge | `verification/clash-verge.md` | not tested | verified / partially verified / documented failure |
| P1 | Stash | `verification/stash.md` | not tested | verified / partially verified / documented failure |
| P1 | Surge | `verification/surge.md` | not tested | verified / partially verified / documented failure |
| P1 | v2rayN | `verification/v2rayn.md` | not tested | verified / partially verified / documented failure |
| P1 | NekoBox | `verification/nekobox.md` | not tested | verified / partially verified / documented failure |

Suggested manual order:

1. Chrome/system proxy on the sepigs host.
2. Mihomo on desktop using `examples/clients/mihomo.yaml`.
3. Shadowrocket on a LAN iOS device with an authenticated listener.
4. Clash Verge using the Mihomo-compatible profile.
5. Stash and Surge using their generated configs.
6. v2rayN on Windows.
7. NekoBox on Android or desktop.

## 3. External Protocol Interoperability

Current status: `0 verified, 0 skipped, 11 blocked, 0 failed`.

Report paths:

- `reports/compat/external-v0.2.0-beta.md`.
- `reports/compat/external-v0.2.0-beta.json`.

Reference implementations to install or detect:

| Implementation | Detection command | Install command | Current status |
| --- | --- | --- | --- |
| shadowsocks-rust | `command -v sslocal && command -v ssserver` | `cargo install shadowsocks-rust` | missing |
| shadowsocks-libev | `command -v ss-local && command -v ss-server` | `brew install shadowsocks-libev` | missing |
| sing-box | `command -v sing-box` | `brew install sing-box` | detected at `/opt/homebrew/bin/sing-box`; blocked until a vetted launcher exists |
| xray | `command -v xray` | `brew install xray` | detected at `/opt/homebrew/bin/xray`; blocked until a vetted launcher exists |
| trojan-go | `command -v trojan-go` | `go install github.com/p4gefau1t/trojan-go@latest` | missing |

After installing reference binaries:

```bash
npm run compat:external
```

Required Shadowsocks cases:

- reference client -> sepigs inbound, `aes-128-gcm`.
- reference client -> sepigs inbound, `aes-256-gcm`.
- reference client -> sepigs inbound, `chacha20-ietf-poly1305`.
- sepigs outbound -> reference server.
- wrong password.
- large payload.
- remote close.

Required Trojan cases:

- reference client -> sepigs inbound.
- sepigs outbound -> reference server.
- wrong password.
- large payload.
- remote close.
- TLS/SNI capability boundary.

## Release Alignment Notes

- `v0.2.0-beta.0` tag target remains `e3b02a65cba7ccee76a37bc9d8f2ef9c6d34c483`.
- `origin/main` includes post-tag acceptance-only documentation/report changes.
- The difference from tag target to `origin/main` contains no runtime code, release packaging, or security fix.
- Keep `v0.2.0-beta.0`; do not create `v0.2.0-beta.1` unless a later acceptance step finds a real bug or release-package/security fix.
