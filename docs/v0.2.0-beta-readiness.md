# v0.2.0-beta Readiness

## Passed

- Strict lint/typecheck/build and complete automated suite.
- TCP and UDP benchmarks plus threshold gate.
- Ten-minute Phase 8 soak and seven-scenario matrix.
- Security scan, subscription redaction, Web build, docs check, verification pack, and beta release audit.
- Phase 9 30-minute mixed gate: 446,889 successes, 239 recoverable errors (0.05345%), 360 reloads, and final resources 0/0/0.
- Ten-minute short soak: 19,071/19,071 successes, 120 reloads, p95 5.40 ms, and final resources 0/0/0.
- TCP/UDP performance gate: all TCP rows within baseline limits; UDP 2,000/2,000 with zero errors.

## Conditional Gates

- External Shadowsocks/Trojan reference tests are skipped because no supported binary is installed; local fixtures are not external certification.
- Initial push of `b162542` is blocked by GitHub credentials: HTTPS lacks `workflow` scope and SSH lacks a key. This is a publication blocker, not a code-validation failure.
- All 239 mixed-gate errors were fake-IP CONNECT read timeouts under reload pressure. The aggregate gate passed, but the concentration requires longer host soak before production-stable status.

## Release Decision

The codebase meets the local validation conditions for `v0.2.0-beta`. Publication remains blocked by repository credentials, and experimental/unsupported classifications remain visible in `phase9-regression-matrix.md`.
