# v0.2.0-beta.0 Readiness

## Passed

- Strict lint/typecheck/build and complete automated suite.
- TCP and UDP benchmarks plus threshold gate.
- Ten-minute Phase 8 soak and seven-scenario matrix.
- Security scan, subscription redaction, Web build, docs check, verification pack, and beta release audit.
- Phase 10 30-minute mixed gate: 402,915 successes, 240 recoverable errors (0.05953%), 318 reloads, and final resources 0/0/0.
- Ten-minute short soak: 16,609/16,609 successes, 104 reloads, p95 4.14 ms, and final resources 0/0/0.
- TCP/UDP performance gate: all TCP rows within baseline limits; UDP 2,000/2,000 with zero errors.

## Conditional Gates

- External Shadowsocks/Trojan reference tests are skipped because no supported binary is installed; local fixtures are not external certification.
- Push of `main` is blocked by GitHub credentials: HTTPS OAuth lacks `workflow` scope and SSH authentication fails with `Permission denied (publickey)`. This is a publication blocker, not a code-validation failure.
- Mixed-gate errors were concentrated in fake-IP CONNECT read timeouts under reload pressure (239) plus one Dashboard metrics `ECONNRESET`. The aggregate gate passed, but the concentration requires longer host soak before production-stable status.
- Full 24-hour soak is pending. Phase 10 completed a two-hour checkpointed `24h` profile segment with 1,220,350/1,220,350 successes, 0 errors, 1,440 reloads, and final resources 0/0/0.
- P0/P1 GUI and mobile clients remain `not tested` and `ready-for-human-signoff`; no unsigned row is promoted to verified.

## Release Decision

The codebase meets the local validation conditions for `v0.2.0-beta.0`. Public release and tag creation remain blocked until repository credentials push `main`; full 24-hour soak and human/external compatibility are explicitly pending rather than claimed.
