# v0.2.0-beta Release Summary

Generated: 2026-06-28 CST

## Local Release State

- Package version: `0.2.0-beta.0`.
- `main` includes post-tag acceptance reports and signoff documentation.
- Local workflow files exist: `.github/workflows/ci.yml`, `.github/workflows/release.yml`.
- Local tag `v0.2.0-beta.0`: present.

## Remote State

- Repository: `https://github.com/Coilcap/sepigs`.
- Default branch: `main`.
- Remote `origin`: `git@github.com:Coilcap/sepigs.git`.
- Remote `main`: present; final signoff documentation is pushed before
  publication.
- Remote tag object `v0.2.0-beta.0`: `1b7077a864317c846e81c8556429c6f08abaf5d2`.
- Remote tag target `v0.2.0-beta.0^{}`: `e3b02a65cba7ccee76a37bc9d8f2ef9c6d34c483`.

## GitHub Actions State

- CI on `main` push: completed successfully, run `28200311591`.
- CI on `v0.2.0-beta.0` tag push: completed successfully, run `28200331953`.
- Release workflow: manually triggered through `workflow_dispatch` on `v0.2.0-beta.0` and completed successfully, run `28200593452`.
- Release workflow automatic tag trigger: not configured; `.github/workflows/release.yml` is `workflow_dispatch` only.

## Publication Status

`v0.2.0-beta.0` is published as a pushed Git tag. Its existing GitHub Release
record is bound to that tag and approved for publication as a prerelease after
the final documentation checks and push.

- Release title: `sepigs v0.2.0-beta.0`.
- Release type: prerelease, not stable, and not latest.
- Published prerelease URL:
  `https://github.com/Coilcap/sepigs/releases/tag/v0.2.0-beta.0`.
- Release binding: API `tagName` is `v0.2.0-beta.0`.
- Stable release: not created.
- Full 24-hour soak: passed with 14,645,605 successes, 11 errors
  (0.00007511%), p95 7.82 ms, 17,303 reloads, and final resources 0/0/0.
- Manual GUI/mobile client sign-off: all P0 and P1 clients verified by user
  manual signoff; missing detailed metadata remains `unknown`.
- External Shadowsocks/Trojan reference interoperability: 0 verified,
  11 blocked, and 0 failed because no vetted automatic launcher exists.

## Current Decision

- Code validation: ready for public beta based on the automated matrix, full
  24-hour soak, security/release checks, and user manual client signoff.
- GitHub publication: publish the existing correctly bound draft as a
  prerelease.
- GitHub Actions: CI and manually dispatched release workflow passed.
- Release tag: created and pushed; do not move it.
- `v0.2.0-beta.1`: not required by current evidence.
- Stable release: not started; this beta is not production-stable.
