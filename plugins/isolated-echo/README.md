# isolated-echo

Example plugin for Phase 6 isolation testing.

It exposes `handle(payload)` and does not request sepigs API permissions. Use it with `plugins.isolation.mode` set to `worker-thread` or `child-process`.
