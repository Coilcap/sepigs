# Shadowrocket

1. Add a SOCKS5 node with server `SEPIGS_HOST`, port `1080`, and the username/password from the sepigs inbound.
2. Enable the node and open `https://example.com/`.
3. Success: page loads, Dashboard records the destination, and disabling the node restores direct networking.
4. Failure checks: same LAN, firewall, authenticated public-bind config, correct credentials, and no VPN profile conflict.

| Device | App version | Import | Request | Rollback | Result | Notes |
| --- | --- | --- | --- | --- | --- | --- |
|  |  | pending | pending | pending | pending |  |
