# Contributing

- Keep protocol logic out of `Engine`.
- Register new inbounds/outbounds through registries.
- Add tests for config validation, failure paths, and resource cleanup.
- Do not add protocols without interoperability notes.
- Use local-only test fixtures by default.
- Document whether a feature is production-ready, experimental, or an interface boundary.
